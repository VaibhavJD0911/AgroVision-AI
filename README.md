# AgroVision-AI
Cattle Breed Identifier is a deep learning–based web application built with Python and Django that identifies cattle and buffalo breeds from images. It uses a CNN model with TensorFlow trained on Kaggle datasets, with image preprocessing via NumPy and Pillow. The app also features a rule-based farmer chatbot for basic livestock queries.
